# This file is part of the stickforstats package test suite.
# Run tests via testthat::test_package("stickforstats") or devtools::test()

library(testthat)
library(stickforstats)

test_check("stickforstats")
