# TODO — as of 2026-07-14

Ordered by *risk to a user's scientific conclusion*, not by effort. Every item says how it was
found and how you will know it is done.

Status key: `[ ]` open · `[~]` in progress · `[x]` done · `[?]` needs verification before it can be
believed either way.

---

## P0 — a user is being shown something false

- [x] **Deploy `main` to production.** ✅ **DONE 2026-07-14 — live is now `cfe14c1`.**
      Verified against the running containers, not a local checkout: t-test power 0.80146 (truth
      0.801460); paired t 0.75396 (not the independent 0.4779); rank power 0.4600362 (not the pre-fix
      0.451351); rank n = 68; ANOVA k=3 f=0.25 → 53/group, 159 total (statsmodels agrees); an
      impossible design returns **HTTP 400**, not a number. Edge returns 401 (Basic Auth), not 502.
      Rollback: `stickforstats/*:rollback-prev` → `7a8dced`. No migrations, so nothing to undo.

- [ ] **The reproducibility receipt is silently unsigned in production.**
      `RECEIPT_RSA_PRIVATE_KEY_B64` / `RECEIPT_RSA_KEY_ID` are in **no** `.env.example`, are not
      fail-closed, and are passed only to `backend` (not `celery`/`celery-beat`). With them unset the
      backend generates an **ephemeral** keypair — and gunicorn runs **4 workers with no `--preload`,
      so each worker mints a DIFFERENT key while advertising the SAME key id**
      (`stickforstats-receipt-ephemeral`). Verification is a coin flip.
      The receipt is the product's moat; right now it does not hold.
      *Done when:* the key is in `.env.example` (documented), fail-closed in compose, passed to all
      three services, and two receipts signed by different workers verify against the same public key.

- [?] **Beta Basic-Auth password — genuinely unknown.** `SESSION_HANDOFF_2026-07-13-POWER.md` says
      NOT rotated; `SESSION_HANDOFF_2026-07-13.md` and `MEMORY.md` say rotated. A `401` for the old
      password proves nothing (the edge 401s any credential). **Do not repeat either claim.**
      *Done when:* someone reads `/root/stickforstats-beta-access.txt` on the host, or authenticates
      with the current secret, and writes the answer down once.

## P1 — the guard rails are not guarding

- [ ] **CI's eslint does not lint a single `.jsx` file.** Executed: CI's exact command visits 293
      files and **zero of the repo's 469 `.jsx` files**, reports 0/0, exits 0 — so `--max-warnings 0`
      passes *vacuously*. Adding `--ext .js,.jsx,.ts,.tsx` surfaces **46 errors and 1030 warnings, all
      in `.jsx`**. Among them `no-use-before-define` fires **7 times** — the exact temporal-dead-zone
      class that crashed the chi-square screen, ML train and CI plot. **The rule was on. CI never
      looked.**
      *Do it in two steps, or the noise will bury you:* (1) add `--ext` and set `--max-warnings` to
      the current count so the gate cannot get *worse*; (2) burn the 46 errors down, then ratchet.
      *Done when:* the 7 `no-use-before-define` errors are zero and the count only goes down.

- [ ] **Lint does not gate the deploy.** `docker-build`'s `needs:` is `[backend-test, frontend-test]`
      only. A red lint job does not stop the GHCR image push. Add the lint jobs to `needs:` once (P1)
      above is green, or the gate is decorative.

- [ ] **21 backend tests fail, and CI cannot see them.** `tests/test_power_analysis_validation.py`
      uses plain classes, not `unittest.TestCase`, so Django's runner — **which is what CI runs** —
      skips the whole file. They fail on clean `main`. They test `core/power_analysis.py`, a **second,
      legacy power engine** that no `urls.py` routes to.
      *Decide and act:* delete the dead engine + its dead `core/api_views.py` power view + the tests,
      **or** fix them and make CI collect them. Do not leave 21 red tests hiding behind a green badge.

- [ ] **`continue-on-error: true`** remains on `sdk-test` (`ci.yml:147`) and the Playwright E2E job.
      Those jobs cannot fail the build. Either make them real or delete them.

## P2 — Class A (silent default) instances still live

See `DEFECTS_AND_PATTERNS_2026-07-14.md` §A. Each is a `||` / `??` / bare `else` on a value that
names a *kind of thing*.

- [ ] `powerAnalysisCodeGenerator.js:177` — `ARE_BY_PARENT[parent] ?? ARE_BY_PARENT.normal`. An
      unknown parent distribution silently gets the **normal** ARE while the prose around it names the
      parent the user chose. (Reachability from the UI is disputed — the dropdown is constrained.
      Fix it anyway; the next caller will not be.)
- [ ] `powerAnalysisCodeGenerator.js:94` — `SAMPLE_SIZE_TABLE[testType] || SAMPLE_SIZE_TABLE['mann-whitney']`.
      An unrecognised rank test silently prints the **Mann-Whitney** table. This is *exactly* the
      defect `TheExportedTableIsPerTestNotPerMannWhitney` was written to kill, still latent one branch
      away.
- [ ] `hp_power_analysis_comprehensive.py:335, :355` — bare `else` in the engine. Unreachable **only**
      because `power_views.py` is its sole production importer and now canonicalises. It is one new
      call site from being live again.

## P2 — Class B (wrong distribution in the browser) instances still live

- [ ] `Lesson09_APrioriVsPostHoc.jsx` carries its **own** hand-rolled `normalCDF` / `normalQuantile`
      — a *third* parallel implementation, independent of the backend engine and of
      `powerCalculations.js`. Route it to `distributionFunctions.js` (pinned against scipy) or to the
      backend.
- [ ] `powerCalculations.js` still contains the broken browser power math (bare `else` in
      `powerTwoSampleTTest`; 100-iteration sample-size caps). It is imported by 4
      simulation/visualization components. **Its `interpretEffectSize` and effect-size algebra are
      genuinely live and now correct** — so delete the *power* functions, not the file.

## P2 — correctness, known and deliberately deferred

- [ ] `ResultDisplay.jsx` renders `p = {formatValue(results.p_value)}` where `formatValue` does
      `value.toFixed(decimals)` — so a tiny p-value prints as **`0.0000`**. Believed live via
      `ANOVACompleteModule.jsx`. (A p-value is never exactly zero; say `< 0.0001`.)
- [ ] Guardian: correlation Pearson→Spearman fallback, and the one-sample t fallback — still out of
      scope from the 2026-07-09 IA audit.
- [ ] **PR C** from the 2026-06-05 Bayesian/p-curve audit: **not started.** Bayesian method-mislabels
      (ANOVA Rouder/BIC citation, ROPE Kruschke HDI rule, correlation integrand label), unseeded RNG.
- [ ] `VisualizationDashboard.jsx:72` imports `./StatisticalDashboard`, which **does not exist**.
      Dead code with a broken import.
- [ ] `PipelineRecorder.jsx:411,440` emit a literal `# TODO: Implement` into **generated code**.

## P3 — hygiene

- [ ] `DEPLOYMENT_RUNBOOK.md` §3.4(a) tells the operator to create `backend/sql/init.sql`. **It now
      exists.** The instruction is obsolete.
- [ ] `JWT_SECRET` is fail-closed in compose and defined in `.env.example`, but **no backend code
      reads it** (`auth_service` reads `JWT_SECRET_KEY`). Dead config that can only mislead.
- [ ] Known host-port collision: `frontend` publishes `${FRONTEND_PORT:-80}:80` while `nginx`
      hardcodes `80:80`. A plain `docker compose up` with `FRONTEND_PORT` unset fails to bind 80
      twice. Runbook works around it (`FRONTEND_PORT=127.0.0.1:8090`); fix the compose file instead.
- [ ] Migrations and `collectstatic` are **not automated** — no entrypoint; the image's build-time
      `collectstatic` is wrapped in `|| true`, so failures are silent.

---

## The rule that has to survive this list

**Mutation-check every regression test you write.** Put the bug back; if the test still passes, it
tests nothing. Seven of the eight adversarial rounds in the power arc were needed because a green
test was mistaken for a working one. And verify the mutation *actually applied* — a substitution that
silently fails to match gives you a green run and a lie.
