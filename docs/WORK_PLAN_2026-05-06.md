# Remediation Work Plan — StickForStats v2.0
## Derived from Critical Review 2026-05-06

**Date created:** 2026-05-06
**Source audit:** [`CRITICAL_REVIEW_2026-05-06.md`](CRITICAL_REVIEW_2026-05-06.md)
**Goal:** Fix or implement every claim made by the project so the audit findings no longer apply. The PI has explicitly chosen the *fix-and-implement* path over the *scope-down* path.
**Owner:** Vishal Bharti (PI), Claude Opus 4.7 (executing agent)

---

## Decisions Already Made (PI confirmed 2026-05-06)

| Question | Decision | Rationale |
|---|---|---|
| Replacement for cherry-picked meta-analysis | **Use real published meta-analysis** (Hackshaw 1998 secondhand-smoke OR `metafor::dat.bcg`) — agent to pick and execute | Removes the cherry-picking concern entirely |
| PLOS validator count: change to 7 or add an 8th? | **Change to "seven"** in PLOS to match code | Lighter touch, code already has 7 strong validators |
| ICH-E9 profile: remove from PLOS or implement? | **Implement** — alias `CLINICAL_TRIAL_PROFILE` as `ICH_E9_PROFILE` and add ICH-E9-specific checklist items | Keeps the discipline-aware-profiles narrative intact |

## Decisions Still Required from PI (deferred)

These need a yes/no with budget/time implications. Phase 5 cannot start until these are answered.

- [ ] **SOC 2 Type II:** pursue real audit (~$50K, 6–12 mo with CPA firm) **or** rewrite docs as honest self-attestation?
- [ ] **FDA Part 11:** pursue real Validation Master Plan + IQ/OQ/PQ + consultant **or** rewrite docs as design specification?
- [ ] **i18n stub languages (id, pl, ru, th, tr, vi):** hire professional translators to complete **or** scope claim to "10 fully translated + 6 navigation-only"?
- [ ] **CRAN submission for R SDK:** pursue (involves CRAN review queue) **or** keep as GitHub-only install?
- [ ] **PyPI publication for Python SDK:** pursue **or** keep as source-install?
- [ ] **Apple/Microsoft signing certificates for desktop:** purchase (annual fees) **or** ship unsigned with caveats?
- [ ] **OSF preregistration:** PI to file the existing draft and supply OSF DOI for the retraction-backtest study **or** strictly reframe pilot as feasibility exercise?
- [ ] **Second coder for retraction backtest:** PI to recruit and have them complete labels **or** publish single-coder pilot with appropriate caveat?

---

## Phase 1 — Scientific Integrity Blockers (1–2 days)

**Exit criterion:** Any reviewer reading the replication directory finds nothing curated and nothing inconsistent.

- [ ] **P1.1** Replace cherry-picked meta-analysis dataset
  - Pick a real published meta-analysis (Hackshaw 1998 OR `metafor::dat.bcg`)
  - Re-run `paper/plos_compbio/figures/generate_figures.py` Case Study 3 with real data
  - Verify Egger's test result (whatever it comes out to be — accept and report)
  - Update PLOS manuscript Case Study 3 prose with new numbers
  - Update Figure 5 with new data
  - Move `find_optimal_meta_data.py` and `create_correct_meta_analysis_data.py` out of the public replication directory (archive under `paper/replication/_pedagogical_seed_search/` with a README explaining they are NOT part of the published case study)
- [ ] **P1.2** Fix PLOS factual errors
  - Change "eight validators" → "seven validators" (lines 20, 62, 113, 115, 248, Fig 4 caption)
  - Either remove ICH-E9 from the discipline-profile list OR implement ICH-E9 alias (per Phase 1 decision: implement alias) — see P1.6
  - Reconcile test count (line 133 vs abstract+222+274). Re-count actual tests and pick one number.
  - Fix endpoint count: change PLOS "197" → actual count from `grep -c "^\s*path(" backend/api/v1/urls.py`
  - Fix page count: change PLOS "40" → actual count
- [ ] **P1.3** Fix `MASTER_VERIFICATION.py` substring bug
  - Replace stdout-substring matching with `result.returncode == 0` check
  - Re-run on all replication scripts; ensure honest pass/fail
- [ ] **P1.4** Update `validate_meta_analysis_paper_data.py` expected-values dict
  - Either align with new real-meta-analysis numbers OR remove this validator if Case Study 3 is replaced
- [ ] **P1.5** Fix JATS empty `<email>` tags (`paper/jats/paper.jats:58-59`)
- [ ] **P1.6** Implement ICH-E9 discipline profile (per decision)
  - Add `ICH_E9_PROFILE` in `backend/core/manuscript/discipline_profiles.py` aliasing `CLINICAL_TRIAL_PROFILE` with ICH-E9-specific checklist items (estimands, intercurrent events, sensitivity analyses)
  - Register in `ALL_PROFILES` list
  - Add a unit test
- [ ] **P1.7** Update MEMORY.md with corrected facts
  - Egger p=0.024 (not 0.010)
  - Guardian validators lines 687-1264
  - File paths under `backend/core/manuscript/`
  - 7 discipline profiles (will be 8 with ICH-E9)
  - 12 Celery tasks
  - ~38 frontend pages / 69 routes
  - Remove `extensions/browser/` reference

---

## Phase 2 — Security & Statistical Correctness (2–3 days)

**Exit criterion:** No auth bypasses, no math bugs that mislead a reader.

### Security
- [ ] **P2.1** Implement JWT signature verification in `sso_service.py:78-121`
  - Use `python-jose[cryptography]` (already a Django/DRF-compatible lib)
  - Fetch JWKS from configured `OIDC_JWKS_URL`
  - Cache keys for 1 hour
  - Verify signature, exp, iat, aud, iss
  - Add tests for: valid token, expired token, wrong audience, forged signature
- [ ] **P2.2** Implement JWT signature verification in `lms_service.py:128-129`
  - Same approach with LTI-specific platform JWKS
  - Add nonce/state storage table for replay protection
  - Add tests
- [ ] **P2.3** Replace `lms_views.py:213-226` placeholder JWKS
  - Generate real RSA keypair, store private key in env, serve public key as JWKS
  - Add management command `generate_lti_keypair`
- [ ] **P2.4** Remove DebugLoginPage credentials
  - Delete `frontend/src/pages/DebugLoginPage.js` entirely (or guard behind a build-time exclusion via webpack DefinePlugin)
  - Remove from `routeConfig.js:173`
- [ ] **P2.5** Implement real plugin runtime sandboxing
  - Refactor `backend/core/services/plugin_runtime.py` to use `multiprocessing.Process`
  - Apply `resource.setrlimit` for CPU + memory inside child process
  - Use `signal.alarm` for hard timeout
  - Block dangerous syscalls via `seccomp` (Linux) or document Linux-only sandbox
  - Custom plugin loading via AST whitelist (no `import os`, `subprocess`, `socket`, `__import__`)
  - OR: scope down the claim — change marketing to "in-process plugin extension API" and document the trust model honestly
- [ ] **P2.6** Fix Stripe sub-processor reference in DPA (or remove if not integrated)

### Statistical correctness
- [ ] **P2.7** Fix `IndependenceValidator` (`guardian_core.py:811-845`)
  - Either replace with `statsmodels.stats.stattools.durbin_watson` on residuals (only meaningful in regression context — would need to reposition this validator)
  - OR rename to `LagAutocorrelationValidator`, drop the "Durbin-Watson" citation, add explicit warning that data must be in time-order, add a real p-value (e.g., via permutation test)
- [ ] **P2.8** Fix Anderson-Darling p-values (`assumption_service.py:227-233`, `guardian_core.py:710-714`)
  - Replace step-function categorical with D'Agostino-Stephens 1986 continuous fit
  - For n>5000, use the actual asymptotic AD distribution rather than two-valued threshold
- [ ] **P2.9** Fix Wilcoxon r effect size (`cascade_engine.py:354-362`)
  - Compute as `Z / √N` per the standard formula
  - Update plain-language and APA-format outputs to use the corrected value
  - Add unit test
- [ ] **P2.10** Fix Kruskal-Wallis ε² label (`cascade_engine.py:394`)
  - Either change formula to `H/(n-1)` (true ε²) OR change label to "η² (unbiased)"
- [ ] **P2.11** Fix genomics module silent failure (`genomics/differential_expression.py:285-289`)
  - Replace `(0.0, 1.0)` fallback with explicit `np.nan` + `flag: "TEST_FAILED"` field
  - Update downstream consumers to handle the flag
  - Add tests for all-tied data, all-NaN data, single-element groups
- [ ] **P2.12** Fix all `except Exception: pass` swallows in critical paths (10 sites identified in audit)

---

## Phase 3 — Implement Claimed Peripheral Services (4–7 days)

**Exit criterion:** Every backend service named in claims actually works end-to-end.

### LMS LTI 1.3 (real implementation)
- [ ] **P3.1** Implement real grade passback POST to LMS AGS endpoint (`lms_views.py:144-173`)
  - Use `requests` with OAuth 2.0 client_credentials flow against platform token endpoint
  - Construct AGS Score JSON per spec
  - Handle response, log to audit trail
  - Add tests with mocked AGS endpoint
- [ ] **P3.2** Add nonce/state storage table for LTI replay protection
- [ ] **P3.3** Add platform-keychain model for storing per-platform JWKS URLs and key cache

### Certification (real implementation)
- [ ] **P3.4** Create DB models: `CertificationLevel`, `Question`, `Exam`, `ExamAttempt`, `CertificationRecord`
- [ ] **P3.5** Migrate hardcoded 10 questions into DB seed; expand to ≥30 questions per level (≥90 total) — document as MVP, not "comprehensive bank"
- [ ] **P3.6** Implement randomized question selection in `certification_service.py:240-269`
- [ ] **P3.7** Implement real certificate signing (HMAC-SHA256 with `CERT_SIGNING_KEY` env var; verify endpoint validates signature against DB record)
- [ ] **P3.8** Implement `check_prerequisites` against real `CertificationRecord` model
- [ ] **P3.9** Implement `UserCertificationsView` against real model

### Site licensing (real implementation)
- [ ] **P3.10** Create DB models: `SiteLicense`, `LicenseUsageRecord`
- [ ] **P3.11** Implement `validate_license_key` against DB
- [ ] **P3.12** Implement `get_license_usage` aggregating from `LicenseUsageRecord`
- [ ] **P3.13** Implement `generate_usage_report` with real numbers
- [ ] **P3.14** Update `SiteLicenseVerifyView` to use real validation

### Frontend mocks
- [ ] **P3.15** Wire RAG dashboards to real Prometheus metrics OR remove routes
  - If wiring: backend Prometheus exporter for query latency, throughput, errors; frontend reads via `/api/v1/metrics/rag/`
  - If removing: delete routes from `routeConfig.js:91,216` and the two component files
- [ ] **P3.16** Replace PCA pathway enrichment with real implementation OR hide section
  - If implementing: integrate with KEGG/Reactome via gseapy or similar; this is substantial work — consider hiding for now
  - If hiding: gate `PcaInterpretation.jsx:164-181` behind a `showPathwayEnrichment={false}` prop
- [ ] **P3.17** Replace `BundleComparison.jsx:296` `Math.random()` with real similarity computation
  - Compute via diff of bundle JSON contents (file count match, parameter match, env match)
- [ ] **P3.18** Replace PowerCalculator G\*Power "validation" with real cross-validation OR remove
  - If real: bundle a small G\*Power equivalent (e.g., `pwr` R package via Rscript subprocess) to actually compare
  - If remove: delete the validation panel; PLOS Table 4 "Within 1%" claim must also be removed
- [ ] **P3.19** Add "DEGRADED" indicator to `useAIAdvisor.js:62-100+` mock fallback path
- [ ] **P3.20** Either implement real demo for `pages/statistics/{DataExploration,StatisticalTests,DataUpload}` or remove the routes
- [ ] **P3.21** Fix `EnvironmentCapture.jsx:198-211` to read from real `package.json` (build-time inject via webpack)

---

## Phase 4 — Mobile / Desktop / SDKs (1–2 weeks)

**Exit criterion:** Every "platform component" claim is backed by working code.

### Mobile
- [ ] **P4.1** Implement `SmartAnalysisScreen` — wire `apiClient.smartProfile` and `autonomousQuery`
- [ ] **P4.2** Implement `PaperCheckScreen` — wire `apiClient.analyzePaper`
- [ ] **P4.3** Implement `GuardianCheckScreen` — wire to Guardian API endpoint with file/text input
- [ ] **P4.4** Implement `CertificationScreen` — wire `apiClient.getCertificationLevels`, `startExam`, `submitExam`
- [ ] **P4.5** Implement `LearnScreen` — basic content navigation
- [ ] **P4.6** Add `LoginScreen` and wire `apiClient.login` (currently declared but no UI)
- [ ] **P4.7** Wire `QuickAnalysisScreen` ANOVA path to actual `runANOVA` (currently falls through to t-test)
- [ ] **P4.8** Make `ProfileScreen` actually persist `user_profile` and `recent_analyses` to AsyncStorage
- [ ] **P4.9** Remove unused dependencies (`react-native-chart-kit`, `react-native-document-picker`, `react-native-fs`) OR use them
- [ ] **P4.10** Wire `ResultsScreen` and `GuardianReportScreen` into navigation flow

### Desktop
- [ ] **P4.11** Generate icon assets (32x32, 128x128, 128x128@2x, .icns, .ico)
- [ ] **P4.12** Decide: webview-shell (honest, low effort) OR add native offline statistics (large effort). Document decision.
- [ ] **P4.13** Configure updater pubkey OR remove updater claim
- [ ] **P4.14** Apple/Windows code signing — depends on PI decision (Phase 5)

### Python SDK
- [ ] **P4.15** Create `sdk/python/tests/` directory with real tests
  - Mock httpx for unit tests
  - Live API tests behind `SFS_TEST_LIVE` env var
  - Cover all wrapper modules
- [ ] **P4.16** Update README to claim Python 3.10+ (matches actual type annotations) OR rewrite annotations for 3.8+ compatibility
- [ ] **P4.17** Remove `pyproject.toml:65-66 testpaths` if not creating tests, OR add tests (preferred)

### R SDK
- [ ] **P4.18** Run `roxygen2::roxygenise()` to generate `man/` documentation
- [ ] **P4.19** Add `withr` and `curl` to DESCRIPTION Imports/Suggests
- [ ] **P4.20** Decide: honestly claim "13 wrappers" OR expand to 40+ tests
- [ ] **P4.21** Run `R CMD check` and resolve all WARNINGs/ERRORs

### Jupyter
- [ ] **P4.22** Reconcile dual `stickforstats-jupyter` packages
  - Keep `sdk/jupyter/` (more complete)
  - Deprecate or delete `extensions/jupyter/`
  - Update memory and any docs that reference the wrong path

### Browser extension
- [ ] **P4.23** Either build `extensions/browser/` (memory references it) OR remove from memory
  - Recommendation: remove memory reference; `sdk/browser-extension/` already covers this

---

## Phase 5 — External-Resource Items (parallel track, awaits PI decisions)

**Exit criterion:** Each item resolved per PI's decision (pursue OR scope down).

- [ ] **P5.1** OSF preregistration: PI files draft, supplies DOI, updates `OSF_PREREGISTRATION.md` with DOI and timestamp
- [ ] **P5.2** Recruit second coder for retraction backtest; complete labels; compute κ; emit `kappa_report.md`
- [ ] **P5.3** Reproducibility re-check on second machine for retraction backtest (PROTOCOL §10.1)
- [ ] **P5.4** Resolve all `[REQUIRES HUMAN VERIFICATION]` flags in `PROTOCOL.md` §16
- [ ] **P5.5** SOC 2: pursue audit OR rewrite as honest self-attestation (PI decision)
- [ ] **P5.6** FDA Part 11: pursue VMP/IQ/OQ/PQ OR rewrite as design specification (PI decision)
- [ ] **P5.7** i18n: hire translators for 6 stub languages OR scope claim (PI decision)
- [ ] **P5.8** CRAN submission for R SDK (PI decision)
- [ ] **P5.9** PyPI publication for Python SDK (PI decision)
- [ ] **P5.10** Apple/Windows signing certs (PI decision)
- [ ] **P5.11** Add prominent "TEMPLATE — Not a binding agreement until executed" header to DPA

---

## Phase 6 — Test Integrity, CI, Cleanup (1–2 days, parallel)

**Exit criterion:** CI is honest about what passes; no orphan files.

- [ ] **P6.1** Fix dead `try: pass` blocks in `test_integration_manuscript.py:49-61`
- [ ] **P6.2** Replace mock-of-self pattern in `test_celery_tasks.py:130-258` with eager-mode integration tests
- [ ] **P6.3** Remove `continue-on-error: true` from SDK and E2E CI jobs (after P4.15-17 and E2E gaps are addressed)
- [ ] **P6.4** Delete `backend/api/v1/audit_views_broken.py`
- [ ] **P6.5** Move `backend/fix_imports.py`, `fix_missing_models.py`, `verify_*.py`, `performance_benchmark.py`, `stress_test_*.py`, `memory_profile_*.py` out of importable production root (e.g., to `scripts/maintenance/`)
- [ ] **P6.6** Rename `backend/test_endpoints.py`, `test_anova_api.py`, `test_high_precision_api.py`, `test_integration.py` to `manual_check_*.py` (they require live server, are NOT real tests)
- [ ] **P6.7** Remove `backend/memory_profile_20250918_160508.json` (captured stack-trace with personal paths)
- [ ] **P6.8** Generate real PWA screenshots for `frontend/public/manifest.json:62-71` OR remove screenshot references
- [ ] **P6.9** Fix README broken links (`docs/GUARDIAN_GUIDE.md`, `DEPLOYMENT_GUIDE.md`, `FEATURES_DOCUMENTATION.md`)
- [ ] **P6.10** Fix README RBAC role name ("Analyst" → "Member" or update code)
- [ ] **P6.11** Update README citation block (JSS desk-rejected; remove or change to JOSS-target)
- [ ] **P6.12** Delete orphan frontend components: `TestTypeScript.tsx`, `ANOVAVisualization.jsx`, `AssumptionFirstSelector.jsx`
- [ ] **P6.13** Remove explicit `linear-gradient(135deg, ...)` strings from `pages/CertificationPage.jsx:342-551` (post 2026-04-17 flat redesign)
- [ ] **P6.14** Fix `examples/biological_datasets/crispr_editing_strategies/README.md:19` row count (10 variants × 4 modalities)
- [ ] **P6.15** Fix `docker-compose.yml` insecure defaults (remove `:-default` for secrets)
- [ ] **P6.16** Fix `kubernetes/production/deployment.yaml:209-210` `ALLOWED_HOSTS=*` to a real list
- [ ] **P6.17** Fix Python version mismatch (Dockerfile 3.9 vs README 3.10+)
- [ ] **P6.18** Fix PLOS Reference 8 (Osborne 2010 Box-Cox) — wrong citation for "434 articles" claim
- [ ] **P6.19** Fix PLOS Table 3 last row to say "documented" not "compliant"
- [ ] **P6.20** Update SOC2 doc endpoint count contradiction (line 56 says 267, README says 195)
- [ ] **P6.21** Fix `validate_against_R.R` static "EXACT agreement" block to reflect actual outcomes

---

## Status Tracker

| Phase | Status | Started | Completed | Notes |
|---|---|---|---|---|
| Phase 1 — Scientific Integrity | **DONE** | 2026-05-06 | 2026-05-06 | All 7 items completed in single session |
| Phase 2 — Security & Math | Not started | — | — | Next up |
| Phase 3 — Implement Services | Not started | — | — | — |
| Phase 4 — Mobile/Desktop/SDK | Not started | — | — | — |
| Phase 5 — External Resources | Awaiting PI decisions | — | — | — |
| Phase 6 — CI/Cleanup | Not started | — | — | — |

### Phase 1 completion summary (2026-05-06)

- **P1.1 Cherry-picked meta-analysis replaced** — Switched Case Study 3 to real Egger 1997 / Sterne 2001 IV magnesium for AMI dataset (`metafor::dat.egger2001`, k=16). New numbers: pooled OR = 0.483, 95% CI [0.329, 0.710], I² = 68.1%, Egger t = -5.78, p < 0.001. Cross-validated R metafor 4.8.0 ↔ Python scipy DL to 4+ decimals via 11 PASS checks. Old `find_optimal_meta_data.py` and `create_correct_meta_analysis_data.py` archived under `paper/replication/_pedagogical_seed_search/` with explanatory README.
- **P1.2 PLOS factual errors** — "eight validators" → "seven" for manuscript review (lines 20, 113, 115, 361); endpoint count 197 → 198 (lines 52, 133); page count 40 → 41 (line 52); test count contradiction reconciled (1,088 = 515+573 in all four mention sites); twelve-trial → sixteen-trial (line 28); CI jobs description corrected (line 228); validator-detection regex updated to match seven actual validators.
- **P1.3 MASTER_VERIFICATION substring bug** — Replaced stdout-substring matching at lines 50-62 with `result.returncode == 0`. Old logic let scripts that printed "PASS" anywhere — even alongside "FAIL" — get marked PASSED. New logic is authoritative.
- **P1.4 validate_meta_analysis_paper_data** — Deleted (was checking against an out-of-sync expected dict; replaced by `verify_meta_analysis_real.py`).
- **P1.5 JATS empty email tags** — Filled both `<corresp>` blocks at `paper/jats/paper.jats:58-59`.
- **P1.6 ICH-E9 discipline profile** — Added `ICH_E9_PROFILE` in `backend/core/manuscript/discipline_profiles.py` extending `CLINICAL_TRIAL_PROFILE` with 4 estimands-specific items (estimand definition, intercurrent events, sensitivity analysis, missing-data assumption). Registered with 5 aliases (`ich_e9`, `ich-e9`, `ich_e9_r1`, `estimands`, `regulatory`). 2 unit tests added to `test_integration_manuscript.py` and both pass.
- **P1.7 MEMORY.md updated** — corrected metrics line, file paths (manuscript/ not services/), Celery task count (12 not 13), Egger p (now <0.001 from real data, not 0.010 stale), 8 discipline profiles, all platform-component honesty disclosures, added today's session log entry.

### New artifacts created this phase

- `paper/replication/data/iv_magnesium_meta_analysis.csv` — 16-row published dataset
- `paper/replication/verify_meta_analysis_real.py` — Python verification script (no R dependency, cross-validated)
- `paper/replication/_pedagogical_seed_search/README.md` — transparency note for archived scripts
- `docs/CRITICAL_REVIEW_2026-05-06.md` — comprehensive audit document
- `docs/WORK_PLAN_2026-05-06.md` — this document

Update this table as phases progress.

---

## Re-Audit Schedule

After each phase, perform a focused re-audit:
- **After Phase 1:** Reviewer-eye check on PLOS manuscript + replication directory
- **After Phase 2:** Security pen-test on auth flows; statistical-correctness check on Guardian outputs
- **After Phase 3:** End-to-end test of LTI launch, certification flow, site license verification
- **After Phase 4:** Build mobile + desktop + install Python/R SDKs from clean checkout
- **After Phase 6:** Full re-run of the 9-agent audit to confirm findings cleared

---

## Companion Document

See [`CRITICAL_REVIEW_2026-05-06.md`](CRITICAL_REVIEW_2026-05-06.md) for the source audit findings.
