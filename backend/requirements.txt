# Core Django dependencies
Django>=4.2.0,<4.3.0
djangorestframework>=3.14.0
django-cors-headers>=4.0.0

# Authentication & Authorization
djangorestframework-simplejwt>=5.3.0  # JWT authentication
python-jose[cryptography]>=3.4.0,<4.0.0  # JWKS-aware JWT verification (SSO + LTI 1.3); >=3.4.0 excludes CVE-2024-33663/33664 (audit 2026-05-31, beta §2)
cryptography>=41.0.0  # RSA keypair generation for LTI tool signing

# Statistical computation - Essential packages only
numpy>=1.24.3
pandas>=2.0.1
scipy>=1.10.1
statsmodels>=0.14.0
scikit-learn>=1.2.2
matplotlib>=3.7.1
seaborn>=0.12.2

# Advanced statistical methods
lifelines>=0.27.0  # Survival analysis (Kaplan-Meier, Cox regression)
factor-analyzer>=0.4.0  # Factor analysis (EFA, rotation methods)
mpmath>=1.3.0  # High-precision arithmetic for critical calculations
pingouin>=0.5.3,<0.6  # ANOVA, post-hoc tests, effect sizes. Pin <0.6: 0.6.x renamed the rm_anova/anova output columns (DF, p-unc) -- the two-way/RM ANOVA code + tests are validated against 0.5.x.
scikit-posthocs>=0.9.0  # Non-parametric post-hoc pairwise tests
pmdarima>=2.0.0  # Auto-ARIMA time series forecasting
prince>=0.13.0  # PCA, MCA, CA dimensionality reduction
networkx>=3.0  # Graph/DAG algorithms for causal analysis

# Visualization
plotly>=5.0.0  # Interactive charts and statistical visualizations

# PDF generation and parsing
reportlab>=4.0.0  # Guardian PDF report generation
PyPDF2>=3.0.0  # PDF text extraction
pdfplumber>=0.10.0  # Advanced PDF parsing

# API documentation
drf-spectacular>=0.27.0  # OpenAPI 3.0 schema generation
drf-yasg>=1.21.0  # Swagger/OpenAPI 2.0 schema

# DRF extensions
drf-nested-routers>=0.94.0  # Nested resource routing

# HTTP client
requests>=2.31.0  # Webhook delivery and external API calls

# Data validation and processing
pydantic>=2.0.0
openpyxl>=3.1.2
xlrd>=2.0.1
pyyaml>=6.0
python-dotenv>=1.0.0

# Data import (v2.0 - SPSS/SAS/Stata support)
pyreadstat>=1.2.0  # SPSS (.sav), SAS (.sas7bdat), Stata (.dta)

# File handling
Pillow>=9.5.0
python-docx>=1.0.0  # Manuscript DOCX parsing
lxml>=4.9.0  # XML/HTML parsing (also JATS-XML manuscript ingestion)

# OCR (optional) — figure images (TIFF/PNG/JPEG) and scanned PDFs.
# Requires system binaries: tesseract (tesseract-ocr) and poppler (poppler-utils).
# Degrades gracefully with a warning if the binaries are absent.
pytesseract>=0.3.10  # Tesseract OCR Python binding
pdf2image>=1.17.0  # rasterize scanned-PDF pages for OCR (needs poppler)

# AI Integration
anthropic>=0.39.0  # Claude API SDK for AI Statistical Advisor

# Async task processing (v2.0)
celery>=5.3.0  # Distributed task queue
redis>=5.0.0  # Celery broker + Django cache backend
django-celery-beat>=2.5.0  # Periodic task scheduling
django-celery-results>=2.5.0  # Task result storage
django-redis>=5.4.0  # Redis cache backend for Django

# Real-time communication (v2.0)
channels>=4.0.0  # WebSocket support
channels-redis>=4.0.0  # WebSocket channel layer backend

# Billing (v2.0)
stripe>=10.0.0  # Subscription and payment processing

# PostgreSQL adapter
psycopg2-binary>=2.9.9  # Use psycopg2 (non-binary) for production builds with custom compilation

# Static file serving
whitenoise>=6.5.0  # Efficient static file serving for Django

# Production server
gunicorn>=21.0.0
gevent>=23.0.0

# Development and testing
pytest>=7.3.1
pytest-django>=4.5.2
